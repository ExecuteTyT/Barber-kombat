// Telegram WebApp injects --tg-theme-* CSS variables automatically when running inside Telegram.
// Fallback values for browser/dev mode are defined in index.css.
export function useTelegramTheme(): void {}
